-create a spec for every major feature that describes user stories, functionality, that sort of thing. I want to document what we THINK the set of features and user flows should be everywhere, so then we can talk about in deatil what they really should be and impleent that. create a new directory that has an index for every major thing then a separate doc with lots of deatils about each thing.






-add property based testing. maybe we already have this?
-add support for weights vs volumes
-security for supabase studio?
-keep recipes under one hour including prep, or make this configurable
-add a way to favorite recipes, and allow seeding a plan with a favorited recipe or selected from so other sort of list.
-we need to verify everything including all sorts of staples.
-when specifying "1 can", also specify the size of the can
-generate PDFs of recipes and evolve the format.
